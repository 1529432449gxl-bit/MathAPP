# MathAPP 前端开发 TODO

> 更新日期：2026-07-20（上一次：2026-07-10）
>
> 适用范围：`frontend/` 用户端，以及与用户端直接相关的 Django API
> 当前判断：P0 已基本清零，P1 的学习闭环（进度同步、个人中心）已完成。剩余阻塞项集中在
> **会员支付前端联调（P1-4）**、**全局搜索（P1-3）** 和 **工程化欠账（P2-2 / P2-3）**。

## 0. 本次核对结论（2026-07-20）

本次按代码实际状态逐条核对了勾选项，结论如下：

- 已确认属实：P0-1、P0-2、P0-3、P1-1、P1-2 对应代码均已存在。
  - 测试文件确认存在：`api.test.js`、`templateParser.test.js`、`learningProgress.test.js`、
    `exerciseProgress.test.js`、`tests/e2e/learning-flow.spec.js`。
  - `progress` 应用（`SectionRecord` / `ProblemRecord`）与 `utils/syncClient.js` 均已落地。
- 需要修正的判断：**P1-4 不是零进度**。后端支付链路其实已经写好（见下方 P1-4），
  真正缺的是前端状态机，原清单把两者混在一起，容易低估已完成的部分。
- 仍为空白：全局搜索（P1-3）后端无对应接口，`content/` 仅有 admin 的 `search_fields`。
- 工程欠账已逐条复验，全部属实未做：路由为静态导入、无 lint 配置、无 CI、
  `frontend/README.md` 仍是 Vite 脚手架原文、`HelloWorld.vue` 无任何引用。

> 说明：本次未能重新执行 `python manage.py test`。仓库内 `.venv/` 为 Windows 环境构建，
> 校验环境为 Linux 且未安装 Django，因此第 1 节的后端验证时间戳仍保留 2026-07-10，
> 未擅自标为新日期。前端测试同理，需在你本机复跑后再更新。

## 1. 当前可用基线

以下能力已经落地，后续默认在此基础上迭代，不再重复建设：

- Vue 3 + Vite 页面框架：首页、知识库、习题库、会员、登录、注册、个人中心。
- Django 内容接口：课程列表、课程详情、按小节懒加载正文，会员正文由后端再次校验权限。
- 后台内容管理：课程、章、小节、发布状态、免费/会员权限、模板正文录入。
- 模板渲染：正文、定义/定理、题目/解析、表格、代码、图片、视频、函数交互及外部交互嵌入。
- 知识库：课程内搜索、章节折叠、上一篇/下一篇、收藏、已读和最近学习。
- 习题库：题型/难度/状态/关键词筛选，已做、做错、收藏和错题本状态。
- 视觉体验：首页、阅读页、刷题页、会员页已完成一轮统一设计和移动端样式适配。

当前学习进度、收藏和错题状态仍保存在浏览器 `localStorage`；它们是可演示版本，不等于完整的登录用户闭环。

### 最近一次基线验证

- [x] `cd frontend && pnpm build`（2026-07-10 通过）
- [x] `python manage.py check`（2026-07-10 通过）
- [x] `python manage.py test`（2026-07-10：4 个后端测试通过；`progress` 应用测试另见 P1-1）
- [x] 建立前端自动化测试（已完成，见 P0-3；本条原为未勾选，与实际不符，本次修正）
- [ ] 在 Chrome、Edge 和手机尺寸下完成一次人工回归
  - Chromium 与 360/768/1024/1440 px 已由 Playwright 自动覆盖。
  - 仍缺：Edge 真机与真实手机浏览器的人工确认。
- [ ] 待办：在本机重跑一次全量验证并回填日期
  （`pnpm test` / `pnpm test:e2e` / `pnpm build` / `manage.py check` / `manage.py test`）

## 2. P0：下一轮必须完成

P0 的目标是得到一个可以稳定验收、可以交给真实用户试用的版本。建议按下面顺序开发。

### P0-1 固化页面地址与恢复能力

- [x] 用路由参数或 query 保存课程、小节和题组，例如：
  - `/knowledge?course=calculus&section=calculus-limits-sequence`
  - `/exercises?course=calculus&section=calculus-limit-basic`
- [x] 刷新页面、复制链接或浏览器前进/后退后，仍能回到同一内容。
- [x] 参数无效、课程已下线或小节已删除时，回退到课程选择页并给出明确提示。
- [x] 快速切换小节时，旧请求不能覆盖新请求结果；使用请求序号与 `AbortController` 双重保护。

验收：打开指定深链接后可直接定位内容；连续快速点击 5 个小节，最终正文始终与选中项一致。

### P0-2 统一接口错误与登录失效处理

- [x] 为 `frontend/src/api.js` 增加可识别的 HTTP 状态和错误类型，而不只返回一段文案。
- [x] `401` 时清理失效登录态，并引导重新登录；保留登录前准备访问的地址。
- [x] `403` 会员内容显示会员锁定组件，不与网络失败混为一类。
- [x] 课程列表、课程目录和正文加载失败时提供“重试”按钮。
- [x] 区分加载中、空内容、无权限、网络错误和内容不存在五种状态。

验收：断开后端、使用过期 Token、访问会员正文和访问不存在的小节时，页面均有正确且可恢复的反馈。

### P0-3 建立最小前端测试体系

- [x] 引入 Vitest，并在 `package.json` 增加 `test` 脚本。
- [x] 为 `templateParser.js` 添加单元测试，覆盖全部模板指令、旧格式兼容和非法输入。
- [x] 为 `learningProgress.js`、`exerciseProgress.js` 添加测试，覆盖写入、切换、清理和损坏缓存恢复。
- [x] 为 API 层添加测试，覆盖 Token、错误响应、空响应和超时/取消请求。
- [x] 增加关键流程浏览器测试：登录 → 打开课程 → 切换小节 → 标记学习状态。

完成情况：Vitest 共 4 个测试文件、15 个用例；Playwright 共 3 个用例，覆盖关键学习流程、360/768/1024/1440 px 响应式和键盘焦点。详细结果见 `docs/FRONTEND_REGRESSION.md`。

验收：`pnpm build` 与 `pnpm test` 都通过；解析器、进度状态和接口错误的核心分支有回归保护。

### P0-4 完成发布前人工回归

- [x] 访客：注册、登录、退出、免费内容、会员内容锁定。
- [ ] 会员：知识正文、会员题组、视频试看/完整播放、权限到期后的页面变化。
  - 已通过知识正文、会员题组、登录后返回原页面和会员到期重新锁定。
  - 未完成：当前已发布内容没有真实视频地址，无法验证实际试看计时和完整播放。
- [ ] 内容：公式、图片放大、视频、表格、代码、题目解析、四类交互模块。
  - 已通过公式、图片放大、表格、题目解析、正弦交互和视频空地址占位状态。
  - 未完成：当前数据库没有已发布的代码块、函数图像、GeoGebra、Desmos 和真实视频示例。
- [x] 学习：课程搜索、收藏、已读、最近学习、上一篇/下一篇。
- [x] 刷题：题型/难度/状态/关键词组合筛选，已做、做错、收藏、加入/移出错题本。
- [x] 响应式：360、768、1024、1440 px；页面无整体横向溢出，筛选栏和长公式已纳入自动回归。
- [x] 可访问性：关键流程 Axe 检查无违规，键盘焦点、表单标签和图片替代文字已验证。

验收：将发现的问题登记为可复现任务；P0/P1 缺陷清零后再进入试运行。

## 3. P1：补齐登录用户闭环

### P1-1 学习状态多设备同步

- [x] 后端增加学习进度、内容收藏、题目状态和错题本的数据模型及 API。（新增 `progress` 应用：`SectionRecord` / `ProblemRecord`，接口挂在 `/api/progress/`）
- [x] 未登录用户继续使用本地状态；登录用户以服务端数据为主。（`syncClient` 仅在有 token 时联网，登录后用服务端快照水合本地）
- [x] 首次登录时询问或自动合并本地数据，避免收藏和错题丢失。（按需求选择“自动合并”：登录后调用 `/api/progress/merge/`）
- [x] 定义冲突规则：布尔状态取并集，最近学习按更新时间排序，删除操作需要可同步。（取消收藏 / 移出错题本 / 清空历史都用布尔置 False 表示，可同步、不物理删除）
- [x] API 失败时保留本地操作并允许稍后重试，页面显示同步状态。（本地离线队列 + 自动重试；个人中心显示“同步中 / 待同步 / 离线 / 出错”）

验收：同一账号在两个浏览器登录，学习记录、收藏和错题一致；离线操作在恢复网络后不会丢失。

后端测试：`progress/tests.py` 覆盖 upsert、清空历史、并集合并、最近学习取较新时间、用户隔离与鉴权；`python manage.py test` 全部通过。

### P1-2 完善“我的”页面

- [x] 展示最近学习，并能继续上次内容。
- [x] 展示课程收藏、习题收藏和错题本，支持直接跳转到原题。
- [x] 展示已学数量、做题数量和错题数量等基础统计。
- [x] 空状态提供清晰入口，不展示无意义的空卡片。
- [x] 修改昵称、机构后及时更新全局用户状态。（`updateProfile` 把返回的用户写回全局 `authState`）

验收：个人中心成为学习记录的统一入口，不需要回到具体课程才能找到收藏或错题。

### P1-3 课程与内容发现

现状：后端没有任何搜索接口，`content/` 下只有 admin 后台的 `search_fields`，
不能复用为前台搜索。前台目前仅有“课程内搜索”。本项需要新增 `/api/search/`。

- [ ] 新增后端全局搜索接口，按标题、知识点、标签检索，并按权限过滤未发布内容。
- [ ] 当课程数量明显增加后，增加跨课程全局搜索。
- [ ] 搜索结果至少包含课程、小节、题组、知识点和标签，并标识免费/会员。
- [ ] 支持从搜索结果直接打开目标小节；地址可复制和刷新。
- [ ] 对无习题课程、空章节和未发布内容提供正确空状态。

验收：用户无需先进入某门课程，即可通过标题、知识点或标签找到目标内容。

### P1-4 会员购买真实环境联调

**这是当前最主要的阻塞项。** 后端已具备，前端状态机基本空白。

已完成（后端）：

- [x] 订单模型与迁移：`core/models.py` 的 `PaymentOrder`（`order_no` 唯一）、`core/migrations/0002_paymentorder.py`。
- [x] 下单与渠道抽象：`core/payments.py` 的 `create_membership_order()` / `create_provider_payment()`。
- [x] 模拟与真实渠道按配置切换：`settings.PAYMENT_PROVIDER`（默认 `mock`，见 `.env.example`）。
- [x] 回调与返回地址：`/api/payments/notify/`、`/api/payments/mock-complete/<order_no>/`、`return_url` 回落到会员页。

待完成（前端与联调）：

- [ ] 处理创建订单、支付中、支付成功、失败、关闭和超时六种状态。
  - 现状：`MembershipPage.vue` 只有 `:disabled="payingPlan === plan.code"` 一个防重复点击判断，
    没有轮询、没有成功/失败/超时分支。
- [ ] 支付成功后刷新当前用户会员状态，并立即解锁正在访问的内容。
- [ ] 轮询在组件卸载或离开页面后必须停止（当前无 `onUnmounted` 清理逻辑）。
- [ ] 生产环境不能出现模拟支付入口；需要在前端按 `mode` 字段隐藏 mock 按钮，
      并确认生产 `.env` 的 `PAYMENT_PROVIDER` 不为 `mock`。
- [ ] 处理 `return_url` 带回的 `?order=` 参数，回到会员页时自动查询该订单最终状态。
- [ ] 补充会员到期、续费和订单异常的用户提示。
- [ ] 为下单与回调补后端测试：重复下单、回调重放、金额或签名不符。

验收：测试环境完成一次从下单到解锁内容的完整流程；失败后可安全重试，不产生重复权益；
同一用户连续点击不产生多个有效订单。

## 4. P2：内容运营与工程质量

### P2-1 后台编辑体验

- [ ] 后台支持图片和视频上传；开发环境使用本地媒体目录，生产环境接对象存储。
- [ ] 上传后自动生成可复制到 `@image` / `@video` 的地址或指令。
- [ ] 为模板正文增加预览，至少能预览 Markdown、LaTeX、图片和题目块。
- [ ] 发布前增加模板语法校验，错误提示定位到具体指令或行号。
- [ ] 记录内容更新时间和编辑人，重要内容支持草稿/发布流程。

### P2-2 性能与包体

- [ ] 路由组件改为动态导入，减少首页首包。
  - 已复验：`frontend/src/router.js` 目前 7 个页面全部为顶部静态 `import`，
    改成 `() => import('./views/XxxPage.vue')` 即可，属低风险改动。
- [ ] 检查 `md-editor-v3` 的引入范围，避免非内容页面加载完整渲染依赖。
- [ ] 对课程目录与正文接口设置合理缓存，并定义后台更新后的失效策略。
- [ ] 图片统一尺寸、WebP/AVIF 和懒加载策略；视频封面避免首屏大资源。
- [ ] 用 Lighthouse 记录移动端性能、可访问性和最佳实践基线。

### P2-3 工程维护

- [ ] 清理 Vite 脚手架遗留文件：未使用的 `HelloWorld.vue`、示例 SVG/图片等。
  - 已复验：`HelloWorld.vue` 在 `frontend/src` 内无任何引用，可直接删除。
    同时确认 `assets/vite.svg`、`assets/vue.svg` 是否仍被使用。
- [ ] 将 `frontend/README.md` 改成项目说明，写清环境变量、启动、构建、测试和接口约定。
  - 已复验：当前仍是 Vite 官方模板原文（开头为 “Vue 3 + Vite / This template should help...”）。
- [ ] 增加 ESLint/Prettier 或等价检查，并提供 `lint` 脚本。
  - 已复验：`frontend/` 下没有任何 eslint / prettier 配置文件，`package.json` 也没有 `lint` 脚本。
- [ ] CI 至少执行 Django check/test、前端 lint/test/build。
  - 已复验：仓库没有 `.github/workflows/`，CI 完全未搭建。
- [ ] 补充 `.venv` 的跨平台说明：当前提交的虚拟环境为 Windows 构建，
      其他平台需自行 `python -m venv` 并 `pip install -r requirements.txt`。
- [ ] 为关键前端错误增加日志上报，但不得记录 Token、密码或支付敏感数据。

## 5. 暂缓事项

以下事项在真实使用数据出现前不进入近期排期：

- 复杂数学表达式自动判题。
- AI 答疑、个性化推荐和自动组卷。
- 为了技术栈而迁移到 Next.js 或全量引入大型 UI 库。
- 四级以上课程目录；只有真实课程结构需要时再扩展。
- 复杂学习报告和排行榜。

## 6. 每个任务的完成标准

任务只有同时满足以下条件才勾选为完成：

- 功能代码和必要的后端接口已完成，不以静态演示或硬编码代替。
- 正常、空数据、无权限、请求失败和移动端场景已处理。
- 新逻辑有相应自动化测试，或在任务中说明暂时只能人工验证的原因。
- `pnpm build`、前端测试、`python manage.py check`、后端测试全部通过。
- 用户可见行为或内容录入方式发生变化时，同步更新相关文档。
- 不提交 `.env`、数据库、日志、`node_modules/`、`dist/` 等本地或生成文件。

## 7. 迭代计划

### 7.1 上一轮迭代（已完成，2026-07-10 至 07-11）

1. [x] 用 URL 保存课程与小节位置，并解决快速切换时的请求竞态。
2. [x] 统一 `401/403/404/网络失败` 的页面反馈和重试入口。
3. [x] 为模板解析器与本地进度状态建立 Vitest 测试。
4. [x] 按 P0-4 清单做一次桌面端和移动端回归（Edge 与真机除外）。
5. [x] 学习状态后端同步（P1-1）与个人中心（P1-2）。

### 7.2 下一轮迭代（建议，按此顺序）

同样只做四件事，优先解锁“能收钱、能验收”这条主线：

1. **打通会员支付前端（P1-4）**：状态机、轮询与卸载清理、成功后刷新会员态、
   生产环境隐藏 mock 入口。后端已就绪，这是投入产出比最高的一项。
2. **补齐内容验收素材（QA-001 / QA-002）**：真实视频、代码块、函数图像、
   GeoGebra / Desmos 示例。没有这些，P0-4 的两条永远无法勾选。
3. **工程化止血（P2-3）**：删除 `HelloWorld.vue`、重写 `frontend/README.md`、
   加 ESLint/Prettier 与 `lint` 脚本、搭最小 CI。改动小但能持续防回归。
4. **路由动态导入（P2-2 首条）**：一次低风险改动即可降低首页首包体积。

全局搜索（P1-3）建议压后：它依赖新的后端接口，工作量明显大于上述四项，
且在课程数量真正变多之前收益有限。

### 7.3 关键风险

- 支付流程在前端未完成前，不应对外试运行；否则用户可下单但页面无法正确解锁。
- 没有 CI 的情况下，已建立的 Vitest / Playwright 测试很容易随改动逐渐失效。
- 内容验收素材缺失会同时阻塞 P0-4 的两条子项，属于“非代码但会卡验收”的依赖。
